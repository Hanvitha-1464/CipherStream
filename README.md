## Run locally

1. Install dependencies with `npm install`
2. Create `client/.env` from `client/.env.example`
3. Start the signaling server with `npm run dev:server`
4. Start the frontend with `npm run dev:client`
5. Open `http://localhost:5173` in two browser tabs or devices

