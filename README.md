


## Run locally

1. Install dependencies with `npm install`
2. Start the signaling server with `npm run dev:server`
3. Start the frontend with `npm run dev:client`
4. Open `http://localhost:5173` in two browser tabs or devices
